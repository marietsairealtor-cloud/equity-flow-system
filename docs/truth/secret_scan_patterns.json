{
  "version": 1,
  "patterns": [
    {
      "name": "supabase-service-role-jwt",
      "pattern": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9",
      "rationale": "Detects Supabase service role JWTs — all share this static header prefix.",
      "false_positive_analysis": "This exact base64 segment is the JWT header for HS256 tokens only; legitimate proof log content (migration hashes, manifest SHA256s) use different encodings and will not match."
    },
    {
      "name": "postgres-connection-string",
      "pattern": "postgres(?:ql)?://",
      "rationale": "Detects Postgres connection strings which may contain embedded credentials.",
      "false_positive_analysis": "Legitimate proof logs do not contain connection strings; gate output and status messages reference DB by name only, not URI."
    },
    {
      "name": "supabase-personal-access-token",
      "pattern": "sbp_",
      "rationale": "Detects Supabase personal access tokens which always begin with sbp_.",
      "false_positive_analysis": "The sbp_ prefix is a Supabase-specific token namespace; no legitimate proof log content uses this prefix in any other context."
    }
  ]
}