PROOF_HEAD=81983e69e2bc4ac4c7f8c1dbea96e5dc5ef584d3
PROOF_SCRIPTS_FILES=scripts/ci_proof_commit_binding.ps1
PROOF_SCRIPTS_FILE_SHA256=scripts/ci_proof_commit_binding.ps1:6f587ea8a5c11be7797cabd6ad10df93383c109a3612a89f356f2b3dd65d9eba
PROOF_SCRIPTS_HASH=ce0178bfa8774469ee797cbb450f48bda03bc02d66100de19ea4e5ea96955ab3
# 10.8.8 Auth Page — Proof

**Date:** 2026-03-26
**Branch:** feat/10.8.8-auth-page
**Gate:** lane-only
**Proof format:** .md (WeWeb UI item — no DB migration)

---

## DoD Verification

### DoD 1 — Auth page exists at /auth
PASS — WeWeb page route confirmed at `/auth`. URL path: `https://b3268184-011c-4ebc-9921-27fc956bb67a.weweb-preview.io/auth`

### DoD 2 — Login (email/password) functional
PASS — Sign In form calls Supabase Auth plugin `Sign In | Email and Password` action. On success navigates to `/post-auth`. Tested with test2@test.com — routed correctly to Today view (active subscription).

### DoD 3 — Signup functional
PASS — Sign Up form calls Supabase Auth plugin `Sign Up` action. On success navigates to `/post-auth`. Tested with test9@test.com — new user created in Supabase Auth, routed to Onboarding (no membership).

### DoD 4 — Password reset functional
PASS — Forgot password flow:
- `/auth` → click "Forgot password?" → navigate to `/reset-password`
- `/reset-password` → user enters email → custom JS calls `publicInstance.auth.resetPasswordForEmail` with `redirectTo: /change-password`
- Reset email received from Supabase Auth
- Email link → lands on `/change-password` with token in URL hash
- New password set via Supabase Auth plugin `Change Password` action
- Sign in with new password confirmed working
- Confirmation message shown after email sent (`reset_email_sent` variable → Message visibility)

### DoD 5 — /post-auth calls accept_pending_invites_v1()
PASS — On page load: AUTH CHECK → `accept_pending_invites_v1()` (no parameters) → `fetch-entitlements`. Tested:
- New user with no invites: accepted_count=0, default_tenant_id=null ✅
- User with pending invite: accepted_count=1, membership created, current_tenant_id set ✅

### DoD 6 — /post-auth calls get_user_entitlements_v1()
PASS — fetch-entitlements workflow calls `get_user_entitlements_v1()` and stores result in `entitlements` variable. Runs after `accept_pending_invites_v1()` on every post-auth load.

### DoD 7 — Entitlement-driven routing
PASS — Three routing paths verified:

| Scenario | Entitlement state | Route |
|---|---|---|
| New user, no invite | NOT_AUTHORIZED / no membership | /onboarding |
| Invited user, expired sub | is_member=true, subscription_status=expired | /onboarding |
| Existing user, active sub | is_member=true, subscription_status=expiring | /today |

WORKSPACE CHECK: `entitlements.data.is_member == true`
SUBSCRIPTION CHECK: `subscription_status == active OR expiring`

### DoD 8 — No direct table calls
PASS — All data calls via Supabase Auth plugin or registered RPCs only. Confirmed in WeWeb logs — no direct table calls observed.

### DoD 9 — No frontend-supplied email parameter
PASS — `accept_pending_invites_v1()` called with zero arguments. Email resolution is server-side via `auth.uid() → auth.users.email`.

---

## Error Handling

- Sign in error: invalid credentials → "Invalid email or password. Please try again." shown below Sign In button
- Sign up error: weak password → "Something went wrong. Please try again." shown below Sign Up button
- Password reset confirmation: "A password reset link has been sent to your email." shown after Send

---

## Phase 3 Verification

- green:once — PASS
- green:twice — PASS
- pr:preflight — PASS

---

## RESULT

10.8.8 Auth Page: PASS (lane-only gate)