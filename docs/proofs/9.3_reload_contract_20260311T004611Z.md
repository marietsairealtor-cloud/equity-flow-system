PROOF_HEAD=03903d0cba7011ec000f106dd292872f69ea4e18
PROOF_SCRIPTS_FILES=scripts/ci_proof_commit_binding.ps1
PROOF_SCRIPTS_FILE_SHA256=scripts/ci_proof_commit_binding.ps1:6f587ea8a5c11be7797cabd6ad10df93383c109a3612a89f356f2b3dd65d9eba
PROOF_SCRIPTS_HASH=ce0178bfa8774469ee797cbb450f48bda03bc02d66100de19ea4e5ea96955ab3
# 9.3 Reload Mechanism Contract — Proof

## Item
Build Route v2.4 — 9.3 Reload mechanism (single canonical path)

## Date
2026-03-11T00:43:49.7864048Z

## Branch
feat/9.3-reload-contract

## Invariant
Only deploy lane may reload PostgREST.
Local harness must never perform or claim reload.

## DoD Verification

### DoD 1: Canonical reload is deploy-lane only and documented
- Canonical path: `docker kill -s SIGUSR1 <postgrest_container>`
- Documented in docs/artifacts/CONTRACTS.md section 19
- SIGUSR1 is the ONLY approved reload mechanism
- Deploy lane stub added to lane_policy.json with reload evidence requirement
- VERIFIED

### DoD 2: Cloud harness includes reload evidence; local harness does not claim reload
- Local harnesses (pgTAP, ci_surface_truth, ci_surface_invariants) run
  against DB or PostgREST without issuing reload signals and make no
  claim that reload occurred.
- SIGUSR1 usage inside test_postgrest_isolation.mjs is strictly
  test-environment setup and does not represent a deploy-lane reload.
  Test-environment setup is not part of the deploy-lane reload contract.
- Cloud harness reload evidence requirement documented in CONTRACTS.md §19
  and enforced via deploy lane stub in lane_checks.json.
- VERIFIED

### DoD 3: Gate enforced in deploy lane + release lane
- VERIFIED — see lane config evidence below.

## Lane Config Evidence

=== lane_checks.json ===
{
  "lanes": [
    {
      "lane": "handoff",
      "checks": []
    },
    {
      "lane": "cloud",
      "checks": [
        "CI / cloud-migration-parity"
      ]
    },
    {
      "lane": "deploy",
      "checks": [
        "reload-evidence-required"
      ]
    },
    {
      "lane": "release",
      "checks": [
        "CI / surface-invariants"
      ]
    }
  ]
}


=== lane_policy.json deploy lane ===
deploy lane:
{
  "lane": "deploy",
  "include_globs": [
    "supabase/migrations/**"
  ],
  "exclude_globs": [],
  "required_checks": [
    "CI / ci-semantic-contract",
    "CI / ci-topology-audit",
    "CI / env-sanity",
    "CI / foundation-invariants",
    "CI / proof-commit-binding",
    "CI / proof-manifest",
    "CI / stop-the-line",
    "CI / test",
    "CI / toolchain-contract",
    "CI / truth-sync-enforced",
    "CI / waiver-debt-enforcement"
  ],
  "notes": "Deploy lane: reload must occur after migration apply. Reload evidence required in proof logs."
}

release lane:
{
  "lane": "release",
  "include_globs": [
    "docs/proofs/**"
  ],
  "exclude_globs": [],
  "required_checks": [
    "CI / ci-semantic-contract",
    "CI / ci-topology-audit",
    "CI / env-sanity",
    "CI / foundation-invariants",
    "CI / proof-commit-binding",
    "CI / proof-manifest",
    "CI / stop-the-line",
    "CI / test",
    "CI / toolchain-contract",
    "CI / truth-sync-enforced",
    "CI / waiver-debt-enforcement"
  ],
  "notes": "Release lane: ci_surface_invariants.mjs must pass after reload. Surface must match expected_surface.json."
}

## Artifacts
- docs/artifacts/CONTRACTS.md §19 — canonical reload contract
- docs/truth/lane_policy.json — deploy + release lane stubs
- docs/truth/lane_checks.json — deploy + release lane checks

## Result
9.3 Reload Mechanism Contract: PASS

