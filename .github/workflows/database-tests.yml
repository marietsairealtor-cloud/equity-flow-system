name: database-tests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  pgtap:
    name: pgtap
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: pgtap (db-heavy stub — converts at 8.0.5)
        run: echo "pgtap stub PASS (db-heavy; converts at 8.0.5 per deferred_proofs.json)"